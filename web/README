## Requirements  
- Para que o projeto funcione execute os seguintes comandos:  
  
`sudo apt-get install nodejs`  
`sudo apt-get install npm`  
`sudo npm install -g gulp`  
  
- na raiz do projeto execute:  
`npm install`  
  
- Para rodar o uglify execute o seguinte comando:  
  
`gulp watch`  
  
## Observação  
Caso ocorra um erro de "node: No such file or directory", executar o seguinte comando:  
sudo ln -s /usr/bin/nodejs /usr/bin/node